## Codex-verdikt mottatt — 9 ULØSTE tråder, sjekkliste

Verdikt: review `5058197255` på `58ce442dd6` (= head ved start, `commit_id` matcher). Ekte Codex-review med tre P2-funn i inline-kommentarer; kroppen er standardteksten. Ikke en driftsmelding.

**Rundetak (§12.1):** seks `## Cursor pre-Codex`-pass ligger på PR-en, altså forbi tre. Men parkeringen taket forutsetter utføres av `cursor-pass-fulgt` (#242), og #242 er **OPEN, ikke merget**. Taket er derfor ikke virksomt ennå — en parkering her ville kommet tilbake som Cursor-funn på neste pass og blitt fikset likevel. Kjent rekkefølge, ikke glemsel. Jeg fikser.

**Kanalvakt (#211):** kjøringen er review-utløst fra `chatgpt-codex-connector[bot]`. Merge er ikke et lovlig utfall her uansett hvordan resten leser. Runden ender med `@cursor review`.

### Verdiktets egne tre funn (review `5058197255`)
- [ ] **P2** `app.py:68` — sitatbudsjettet antar at utsnitt er disjunkte
- [ ] **P2** `app.py:158` — `slipp_gjennom` er O(n) per kall ved 50 000-vinduet
- [ ] **P2** `controller.py:456` — 429 fra fornyelsesproben leses som lease-tap

### Eldre ULØSTE bot-tråder (skal fikses, ikke merges forbi — #211)
- [ ] **P2** `app.py:2526` — lease revalideres ikke ved skrive-/commit-grensen
- [ ] **P2** `app.py:2587` — ZIP-filnavn måles i UTF-8, ikke arkivets egen koding
- [ ] **P2** `app.py:2721` — `json.dumps` escaper NUL, så predikatet ser den aldri
- [ ] **P2** `kjoring.py:297` — `TemporaryDirectory` reiser `OSError` utenfor `try`
- [ ] **P2** `kjoring.py:610` — `cleanup()` i `finally` kan overskrive kodet feil
- [ ] **P2** `app.py:2781` — tomt intervjuspørsmål-innslag okkuperer PK-en permanent

### Står ÅPEN med vilje
- **P1** `app.py:2540` — strømmede artefakter isoleres ikke fra senere claim-generasjoner. §9 K1-stopp, eskalert til @moka1980 12:50. Krever arkitekturvalg (a) eller (b) — ikke rørt i denne runden, og tråden lukkes ikke: et levende P1 skal holde PR-en umergbar til dommen faller.

Jeg pusher én commit per funn og krysser av underveis.
