## Implementørens sjekkliste — Codex-review av `d6c1075eae`

Tre funn, alle P2, alle verifisert i koden på denne headen. Jeg tar dem
ett av gangen (fiks → commit → push) og krysser av her etter hver push.

- [ ] **P2** `027_varselsender.sql:283` — `varsel_rekoe` løfter en gjenopptatt
      rad til `koet` uten å spørre om den er LEST. `pensjoner_runde` og
      `merk_lest` lar med vilje `under_sending` stå (`I_KO` er
      `('koet','feilet')`), så en rad som ble lest mens den var klaimet, og
      som deretter kommer tilbake gjennom leasen, står `koet` for alltid:
      klaimet krever `lest_ts IS NULL`, og `varsel_rekoe` rører aldri `koet`.
      Skal til `ikke_aktuelt`, samme vei som avmeldingen alt går.
- [ ] **P2** `disponit-varselsender.service:49` — `TimeoutStartSec=5min` mot en
      kjøring som i verste fall er 50 SMTP-kall à 20 s. En legitim kjøring kan
      drepes midt i bunken, og lander drapet mellom et akseptert SMTP-kall og
      statusoppdateringen, sender leasen den samme e-posten om igjen.
      Grensen og fristen må høre sammen.
- [ ] **P2** `026_varsel.sql:48` — ingen backfill for runder som alt står `apen`
      eller `klar` ved oppgraderingen. De eneste produksjonsveiene som lager
      varsler er en NY runde og en replay av samme forespørsel, så nettopp den
      ventende jobben modulen finnes for, blir usynlig etter deploy.

CI er grønn på `d6c1075` (1225 python-tester, 0 hoppet over).
