## Implementørens sjekkliste — Codex-review av `8a25c3a446`

Seks reelle funn, alle bekreftet i koden. Jeg tar dem ett av gangen (fiks → commit → push) og krysser av her etter hver push.

- [x] **P1** `policyadmin_http.py:267` — tenantkonteksten er borte: `_browserkontekst` gjør `conn.rollback()`, og `varsel`-funksjonene setter aldri `sett_kontekst`. Med FORCE RLS gir det tom innboks og en `sett_kanal` som avvises. — `c1148d7`
- [x] **P1** `policyadmin_http.py:294` — `/lest` og `/v1/varselvalg` committer aldri; `gi_tilbake` ruller ubetinget tilbake, så begge svarer «ok» og kaster endringen. — `53b7ed9`
- [x] **P2** `policyadmin_http.py:267` — innboksens GET krever CSRF-header; `hentJson` sender bare `Accept`, så flaten får `403 csrf_ugyldig`. Skal bruke `_leseauth`. — `2bc2034`
- [ ] **P2** `varsel.py:195` — ingenting pensjonerer et varsel når handlingen er utført: attestanten ser fortsatt «venter på deg», og resten står uleste etter at runden er lukket.
- [ ] **P2** `varsel.py:250` — bytte til `kun_portal` etterlater alt køet på `epost_status='koet'`, så avmeldingen kommer for sent for e-post som alt ligger i kø.
- [x] **P2** `varsel.py:195` — `varsel.attestering_venter` mangler i `locales/nb.json` / `locales/en.json`. Alt løst av eiers `97ecc98` (portalinnboksen), som la nøkkelen inn i begge språk med samme fire parametre som tjenesten sender.

Merk: grenen fikk nye commits fra eier mens runden pågikk (`97ecc98` m.fl.); arbeidet er rebasert på dem.

Ber om ny `@codex review` når alle seks er pushet.
