## Runde 2 — de to gjenstående P2-ene fra pre-Codex-passet

Passet (`github-actions[bot]`, SHA `13c12c5513`) bar tre P2. Runde 1 lukket
P2-1 (generasjonsracet, `++eval2.nr` i `rekruttering.js:1350`) sammen med
Codex-funnene, men **P2-2 og P2-3 sto igjen ulukket** — begge verifisert i
koden på `94f9a8f` før denne runden, ikke antatt.

- [x] **P2-2** `lesing.py:646,712` — evalueringscursoren var v1, som binder
  BARE tenant. `cursor.les` godtar også en v2-kropp (`t`/`ts`/`id`), så en
  gyldig cursor fra `beslutninger` eller `/v1/unntak` hos samme tenant var
  200 her og forskjøv keysetet til en fremmed `(ts, id)`-posisjon. Byttet til
  `les_v2`/`lag_v2` med `endepunkt="rekruttering_evalueringer"`,
  `retning="desc"`, `filtre={}` — husformen fra søsknene i samme modul.
  Ingen ny maskin (K1). → `03e3f9e`
- [x] **P2-3** `test_m57_rapportflate.py:517` — den negative armen var bare
  `cursor=AAAA.bbbb`, som beviser signaturkontrollen og ikke tenantleddet.
  La til husformen fra `test_api.py::test_cursor_ugyldig`: en ekte, korrekt
  signert `ANNEN_TENANT`-cursor, pluss en gyldig nabo-cursor fra
  `beslutninger` hos samme tenant. Begge 400 `cursor_ugyldig`. → `f6137c8`

Navnet er `rekruttering_evalueringer` og ikke `rekruttering.evalueringer`
som funnet foreslo — husformen i `cursor.py`-konsumentene er snake_case
(`unntak_historikk`, `domeneovertakelse_saker`, `beslutninger`). Bindingen er
den samme; bare strengen følger naboene.

P3-ene fra passet: den første (stille listefeil) ble lukket i `8691da3`; den
andre følger av P2-1-fiksen, som passet selv noterte.

Verifiseringsrunden som ble startet 10:18 målte `94f9a8f` og er innhentet av
disse to pushene — den erstattes av runden under, på `f6137c8`.

Lokalt: bindingene er kjørt direkte mot `cursor.les_v2` (egen cursor OK;
fremmed tenant, nabo-endepunkt og v1-kropp alle avvist). DB-armene krever
Postgres og kjører i CI.

@cursor review
