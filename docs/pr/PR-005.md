# PR-005: API-inngang + M-37-lagring (mot v2 + v3-delta + GO-vilkår)

**Draft: Claude.ai · GO fra ChatGPT (andre runde) · Branch: `pr-005-m37-api`**

## Hva draften inneholder — og verifiseringsstatus

| Artefakt | Status |
|---|---|
| `platform/core/db/migrations/003_unntak_api_policy.sql` | **Kjørt mot ekte PostgreSQL 16 lokalt.** 10 kontrakter røyk-verifisert: GO-vilkår 1 (destruert+aktiv avvises av CHECK), historikk-trigger m/ aktor fra serverkontekst, kryss-tenant-FK avvises, RLS, statusmaskin, aktor-påkrevd (tx feiler uten), jti-replay=unikbrudd, verifiser_token m/ format-guard, claim-historikk. Idempotent (kjørt 2x). |
| `platform/core/db/kjorer.py` | Herdet kjører: advisory-lås, checksum, kun manglende versjoner, avviser endret historikk og BEGIN/COMMIT i ≥003, legacy-håndtering av 001/002 |
| `deploy/staging/migrasjon-bootstrap.py` | Bootstrap per v3-delta 1: **Claude Code fyller inn de to reviewede checksums fra main 679ee9e — verifiseres i Codex-review (merge-port)** |
| `platform/core/db/kryptering.py` | Envelope AES-256-GCM, DEK-register, crypto-shredding i én UPDATE |
| `platform/core/api/kjerne.py` | Transaksjonsflyt som nummerert kontrakt (11 steg) — Claude Code implementerer; Codex verifiserer transaksjonsgrensene mot denne |
| `platform/core/api/app.py` | Boot-sjekker + endpoint/middleware-kontrakt — Claude Code implementerer mot v2 Del 3-4 |

## Gjenstår for Claude Code (implementeringsfasen)

1. Fyll inn reviewede checksums i bootstrap (merge-port: Codex verifiserer mot main 679ee9e)
2. `attestering.py` v2: bindingsfelter (tenant_id, handling, vilkaar, ressurs_id, policy_id, utstedt, utloper, jti) + `kontroller_binding()`; API-veien avviser PR-004-format
3. Implementér kjerne.behandle() og app.py mot kontraktene; erstatt `migrer` i pg.py med kjorer.migrer
4. Full testsuite per v2 Del 8 + v3-testtillegg — én test per feilveitabellrad, 20-tråders idempotens- og jti-kappløp, SECURITY DEFINER-negativtester
5. `deploy/staging/lasttest-m01.py` per v2 Del 6 (warmup 10 s, 6000 målte, open-loop 100/s, artefakt-JSON)
6. m01-manifest til nytt strukturert format (v3-delta pkt. 7) — manifestskjema + CI-validering
7. CI: `cryptography` i pip-install; deploy-skript oppdateres (runtime-rolle `disponit_runtime` + authenticator + pepper/KEK i miljøfil) — deploy-skript HAR CI-dekning nå, hold den grønn

## Codex-verifikasjonsliste (fra GO)

Transaksjonsgrenser · RLS/rolleprivilegier · samtidig idempotens ·
jti-kappløp · crypto-shredding · migrasjonschecksums · sikkerhetssaker
aldri i normal kø · hver negativ test dreper sin vakt.
