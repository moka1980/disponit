# PR-003: Rydding + ADR-001

**Draft: Claude.ai · Utføres av: Claude Code · Branch: `pr-003-rydding-adr001`**

Ren opprydding pluss én arkitekturbeslutning. **Ingen kodeendringer i
motoren** — PR-en rører ikke filer Claude Code endret i PR-002-rundene,
så det er null konfliktrisiko mot main (3bcf368).

## Endringer

**Slettes (verifisert til stede på main via raw.githubusercontent):**
- `platform/core/policy_validator/engine_v01_deprecated.py`
- `platform/core/policy_validator/schema_v01_deprecated.py`
- `policies/policy-schema-v0.1.yaml`

**Nye filer:**
- `docs/beslutninger/ADR-001-revisjonslogg-i-postgresql.md` — vedtaket
  Claude Code ba om: M-2-loggen til PostgreSQL i PR-004, fil-logg
  degradert til KUN UTVIKLING, pluss fire bindende PR-004-krav
  (atomisk SQL-reservasjon, logg+reservasjon i samme transaksjon,
  kryptografisk attestasjonsverifikasjon før API-et åpnes,
  tenant-isolasjon i skjemaet fra første migrasjon).

**Oppdatert:**
- `docs/README-arbeidsflyt.md` — tittel, fjernet utdatert
  sletteanmerkning, «Første konkrete byggeoppgave» erstattet med
  «Status og neste steg» (PR-001/002 levert; PR-003/004/005 + malutvidelse
  som løype). Status-notatet om merge-porten er bevart uendret.

## Instruks til Claude Code

```bash
git checkout main && git pull
git checkout -b pr-003-rydding-adr001
git rm platform/core/policy_validator/engine_v01_deprecated.py
git rm platform/core/policy_validator/schema_v01_deprecated.py
git rm policies/policy-schema-v0.1.yaml
# pakk ut pr003-pakken i repo-rot (docs/-filene)
git add docs/
git commit -m "PR-003: slett v0.1-deprecated, vedta ADR-001, oppdater arbeidsflyt-status"
git push -u origin pr-003-rydding-adr001
# åpne PR med denne filen som beskrivelse
```

Verifiser før merge: `grep -r "_v01_deprecated\|policy-schema-v0.1" --include="*.py" --include="*.yml" --include="*.md" .`

**Korrigert av Claude Code etter Codex-review:** påstanden over sa at treffene
kun skulle finnes under `docs/pr/`. Det stemte ikke. De faktiske treffene
etter denne PR-en er:

- `docs/pr/PR-002.md` og `docs/pr/PR-003.md` — historikk, skal stå
- `platform/core/tests/test_engine.py` — ett deklarert unntak i
  encoding-testen (`if "_v01_deprecated" in fil.name: continue`). Linjen er
  død etter slettingene, men å fjerne den ville endret en testfil, og porten
  for denne PR-en sier «ingen tester endret». Ryddes i PR-004, der testfilen
  uansett skal røres.

Ingen kode og ingen CI-steg refererer de slettede filene — det er kravet, og
det er oppfylt. Deretter: CI grønn (62 tester uendret — ingen kode er rørt).

## Codex-porter

- [ ] CI grønn, ingen tester endret
- [ ] De tre filene er borte; ingen gjenværende referanser utenfor docs/pr/
- [ ] ADR-001 komplett med de fire bindende PR-004-kravene
- [ ] Ingen endringer i platform/core utover slettingene
